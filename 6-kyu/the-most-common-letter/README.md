<h2><a href=https://www.codewars.com/kata/5a434a9dc5e284724f000011/train/javascript target="_blank">The most common letter</a></h2><h3>6 kyu</h3><p>Find the most common letter (not a space) in the given <code>string</code> (comprised of at least 3 lowercase words) and replace it with the given <code>letter</code>.</p><p>If such letters are two or more, choose the one that appears earliest in the <code>string</code>.</p><p>For example:</p><pre><code class="language-java">(<span class="cm-string">'my mom loves me as never did'</span>, <span class="cm-string">'t'</span>) <span class="cm-operator">=&gt;</span> <span class="cm-string">'ty tot loves te as never did'</span>(<span class="cm-string">'real talk bro'</span>, <span class="cm-string">'n'</span>) <span class="cm-operator">=&gt;</span> <span class="cm-string">'neal talk bno'</span>(<span class="cm-string">'great job go ahead'</span>, <span class="cm-string">'k'</span>) <span class="cm-operator">=&gt;</span> <span class="cm-string">'grekt job go khekd'</span></code></pre>